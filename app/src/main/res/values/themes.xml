<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.UniMarketGuajira" parent="Theme.Material3.Light.NoActionBar">
        <!-- Force Light Mode colors -->
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="android:windowBackground">@color/gray_light</item>
    </style>

    <style name="Theme.UniMarketGuajira" parent="Base.Theme.UniMarketGuajira" />

    <style name="App.CustomIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@android:color/transparent</item>
    </style>

    <style name="BottomNavStyle">
        <item name="android:textSize">12sp</item>
    </style>
</resources>